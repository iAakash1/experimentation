# DoRA (weight-decomposed LoRA). Provided for forward-compatibility of the
# config schema. NOTE: the installed backend (mlx-vlm 0.6.3) does NOT implement
# DoRA — `plantdx train` fails closed with a clear message if this is selected.
# Use qlora or lora until the backend gains DoRA support.
method: dora
rank: 16
alpha: 32
dropout: 0.05
train_vision: false
target_modules: [q_proj, k_proj, v_proj, o_proj]
