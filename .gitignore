# ---------------------------------------------------------------------------
# PlantDx .gitignore
# ---------------------------------------------------------------------------

# --- Generated pipeline artifacts (outputs; regenerable from DKB + configs) ---
# The artifacts/ tree mirrors caption_framework/06_folder_structure_spec.md.
# Only .gitkeep / README.md placeholders are tracked; generated content is not.
artifacts/**
!artifacts/
!artifacts/**/
!artifacts/**/.gitkeep
!artifacts/**/README.md

# --- Datasets (large; not redistributed; retained by original authors) ---
tomato/raw/**
mango/raw/**
!tomato/raw/.gitkeep
!mango/raw/.gitkeep

# --- Model checkpoints / adapters ---
*.safetensors
*.gguf
*.npz
checkpoints/
adapters/

# --- Dataset audit reports (generated; regenerable via `plantdx audit`) ---
reports/

# --- Normalized datasets (generated; regenerable via `plantdx normalize`) ---
# Anchored to the repo root so it does not match artifacts/datasets/.
/datasets/

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
.installed.cfg
*.egg
pip-wheel-metadata/

# --- Virtual environments ---
.venv/
venv/
env/
ENV/

# --- Testing / coverage / typing caches ---
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
.mypy_cache/
.ruff_cache/
.hypothesis/

# --- Docs build ---
site/
docs/_build/

# --- Editors / OS ---
.DS_Store
.idea/
.vscode/
*.swp
*~

# --- Env / secrets ---
.env
.env.*
!.env.example
